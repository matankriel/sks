# Cluster-specific overrides for kyverno-policies.
# Site-level defaults are inherited from sites/site-a/kyverno-policies.yaml.
policies:
  routeDnsManagement:
    clusterName: "cluster-a"   # CNAME target: <name>-<ns>.apps.cluster-a.<baseDomain>

  # Override individual policy settings for this cluster as needed, e.g.:
  # minimumReplicas:
  #   minReplicas: 2
